<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Baklovah Admin - Login</title>
    <!-- Apple-inspired design - simple and modern -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <style>
        :root {
            --primary-color: #0071e3;
            --secondary-color: #86868b;
            --background-color: #f5f5f7;
            --card-background: #ffffff;
            --text-color: #1d1d1f;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0 20px;
        }
        
        .login-container {
            max-width: 400px;
            width: 100%;
        }
        
        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }
        
        .card-header {
            background-color: var(--card-background);
            border-bottom: none;
            text-align: center;
            padding-top: 30px;
            padding-bottom: 0;
        }
        
        .card-body {
            padding: 40px;
        }
        
        .form-control {
            border-radius: 10px;
            padding: 12px 15px;
            font-size: 16px;
            border: 1px solid #e1e1e1;
            background-color: #f7f7f7;
        }
        
        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(0, 113, 227, 0.25);
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            border-radius: 10px;
            padding: 12px 15px;
            font-weight: 500;
            width: 100%;
        }
        
        .btn-primary:hover {
            background-color: #0062c3;
            border-color: #0062c3;
        }
        
        .logo {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
            background-color: var(--primary-color);
            border-radius: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 40px;
            font-weight: bold;
        }
        
        .alert {
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="card">
            <div class="card-header">
                <div class="logo">B</div>
                <h2>Baklovah Admin</h2>
                <p class="text-muted mb-4">Login to access the admin panel</p>
            </div>
            <div class="card-body">
                <!-- Display error message if there is one -->
                <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger mb-4" role="alert">
                    <%= error %>
                </div>
                <% } %>
                
                <form id="loginForm" method="post" action="/admin/login" onsubmit="document.getElementById('submitStatus').innerText = 'Submitting login...'">
                    <div class="mb-4">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                    </div>
                    <div class="mb-4">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Login</button>
                </form>
            </div>
        </div>
    </div>

    <div id="submitStatus" class="mt-3 text-info"></div>
    
    <!-- JavaScript for form handling -->
    <script>
        // We'll use simple form submission without AJAX
        // The browser will handle the redirect naturally
        console.log('Login form loaded and ready');
    </script>
</body>
</html>
